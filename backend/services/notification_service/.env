# .env for notification_service
# Update the values below to match your PostgreSQL and SMTP credentials.
# If your DB password contains special chars (e.g. @, :, /), percent-encode them (e.g. @ -> %40).

DATABASE_URL=postgresql+psycopg2://postgres:mohamed123@localhost:5432/notification_db

# SMTP settings (used by notification_service)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=smtp-user
SMTP_PASSWORD=smtp-password
SMTP_FROM=no-reply@example.com
